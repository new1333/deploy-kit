name: selftest
# 诊断 round2：不带 secrets: inherit（deploy.yml 已去掉 secrets/permissions 定义）
on: workflow_dispatch
jobs:
  call:
    uses: new1333/deploy-kit/.github/workflows/deploy.yml@main
    with:
      project: selftest
      dockerfile: nonexistent.Dockerfile
      app_target: runner
